[general]
static=yes
writeprotect=no

[default]
exten => 1000,1,Answer()
 same => n,Playback(hello-world)
 same => n,Hangup()

 [default]
exten => 9999,1,Answer()
 same => n,Playback(demo-congrats)
 same => n,Hangup()

exten => 1000,1,NoOp(AI Assistant)
 same => n,Answer()
 same => n,Stasis(ai-assistant)
 same => n,Hangup()